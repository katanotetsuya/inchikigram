<!-- コメント入力フォーム ------------------------------------------------------------>
<%= form_with(model: [post, comment], url: post_comments_path(post) ) do |f| %>
    <%= f.text_area :content, class: "comment-area", placeholder: "コメント追加..." %>
    <%= f.submit "送信", class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>